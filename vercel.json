{
  "crons": [{
    "path": "/api/cron",
    "schedule": "0 0 * * *"
  }],
  "buildCommand": "next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/cron/**": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
} 